---
// 传入 props
const { src, width = "600", height = "auto" } = Astro.props;
---

<video width={width} height={height} controls>
  <source src={src} type="video/mp4" />
  Your browser does not support the video tag.
</video>