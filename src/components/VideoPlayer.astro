---
// VideoPlayer.astro
const { src, width = 640, height = 360, controls = true } = Astro.props;
---

<video
  src={src}
  width={width}
  height={height}
  controls={controls}
  preload="metadata"
>
  Sorry, your browser doesn't support embedded videos.
</video>
